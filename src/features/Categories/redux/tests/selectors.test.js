import { selectCategories } from './selectors';
import initialState from './initialState';

describe('test categories selectors', () => {
  it('should return true', () => {
    expect(true).toBe(true);
  });
});
